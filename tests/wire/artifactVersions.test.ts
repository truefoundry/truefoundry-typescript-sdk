// This file was auto-generated by Fern from our API Definition.

import * as TrueFoundry from "../../src/api/index";
import { TrueFoundryClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("ArtifactVersionsClient", () => {
    
    test("list", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueFoundryClient({ "maxRetries" : 0 , "apiKey" : "test" , "environment" : server.baseUrl });
        
        const rawResponseBody = { "data" : [ { "created_at" : "2024-01-15T09:30:00Z" , "updated_at" : "2024-01-15T09:30:00Z" , "manifest" : { "metadata" : { "key" : "value" } , "type" : "artifact-version" , "source" : { "type" : "truefoundry" } , "step" : 1 } , "id" : "jqfwg345gi25n5ju2yz5iz6m" , "fqn" : "fqn" , "created_by_subject" : { "subjectId" : "subjectId" , "subjectType" : "user" } , "ml_repo_id" : "ml_repo_id" , "usage_code_snippet" : "usage_code_snippet" , "tags" : [ "tags" ] , "artifact_id" : "artifact_id" } ] , "pagination" : { "total" : 100 , "offset" : 0 , "limit" : 10 } };
        
        server
            .mockEndpoint({ once: false })
            .get("/api/svc/v1/artifact-versions").respondWith()
            .statusCode(200).jsonBody(rawResponseBody)
                .build();

        
                        
                const expected = {
    data: [{
            createdAt: new Date("2024-01-15T09:30:00.000Z"),
            updatedAt: new Date("2024-01-15T09:30:00.000Z"),
            manifest: {
                metadata: {
                    "key": "value"
                },
                type: "artifact-version",
                source: {
                    type: "truefoundry"
                },
                step: 1
            },
            id: "jqfwg345gi25n5ju2yz5iz6m",
            fqn: "fqn",
            createdBySubject: {
                subjectId: "subjectId",
                subjectType: "user"
            },
            mlRepoId: "ml_repo_id",
            usageCodeSnippet: "usage_code_snippet",
            tags: ["tags"],
            artifactId: "artifact_id"
        }],
    pagination: {
        total: 100,
        offset: 0,
        limit: 10
    }
};
                const page = await client.artifactVersions.list({
    limit: 10,
    offset: 0,
    tag: "tag",
    fqn: "fqn",
    artifactId: "artifact_id",
    mlRepoId: "ml_repo_id",
    name: "name",
    version: 1,
    runIds: ["run_ids"],
    runSteps: [1.1],
    includeInternalMetadata: true
});
                
                            expect(expected.data).toEqual(page.data);
                            expect(page.hasNextPage()).toBe(true);
                            const nextPage = await page.getNextPage();
                            expect(expected.data).toEqual(nextPage.data);
                        
                
                    
    });
          
    test("apply_tags", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueFoundryClient({ "maxRetries" : 0 , "apiKey" : "test" , "environment" : server.baseUrl });
        const rawRequestBody = { "artifact_version_id" : "artifact_version_id" , "tags" : [ "tags" ] };
        const rawResponseBody = { };
        
        server
            .mockEndpoint()
            .put("/api/svc/v1/artifact-versions/tags").jsonBody(rawRequestBody)
                .respondWith()
            .statusCode(200).jsonBody(rawResponseBody)
                .build();

        
                        
                                const response = await client.artifactVersions.applyTags({
    artifactVersionId: "artifact_version_id",
    tags: ["tags"]
});
                                expect(response).toEqual({});
                              
                    
    });
          
    test("get_signed_urls", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueFoundryClient({ "maxRetries" : 0 , "apiKey" : "test" , "environment" : server.baseUrl });
        const rawRequestBody = { "id" : "id" , "paths" : [ "paths" ] , "operation" : "READ" };
        const rawResponseBody = { "data" : [ { "path" : "path" , "signed_url" : "signed_url" } ] };
        
        server
            .mockEndpoint()
            .post("/api/svc/v1/artifact-versions/signed-urls").jsonBody(rawRequestBody)
                .respondWith()
            .statusCode(200).jsonBody(rawResponseBody)
                .build();

        
                        
                                const response = await client.artifactVersions.getSignedUrls({
    id: "id",
    paths: ["paths"],
    operation: "READ"
});
                                expect(response).toEqual({
    data: [{
            path: "path",
            signedUrl: "signed_url"
        }]
});
                              
                    
    });
          
    test("create_multi_part_upload", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueFoundryClient({ "maxRetries" : 0 , "apiKey" : "test" , "environment" : server.baseUrl });
        const rawRequestBody = { "id" : "id" , "path" : "path" , "num_parts" : 1.1 };
        const rawResponseBody = { "data" : { "storage_provider" : "S3_COMPATIBLE" , "part_signed_urls" : [ { "path" : "path" , "signed_url" : "signed_url" } ] , "finalize_signed_url" : { "path" : "path" , "signed_url" : "signed_url" } , "s3_compatible_upload_id" : "s3_compatible_upload_id" , "azure_blob_block_ids" : [ "azure_blob_block_ids" ] } };
        
        server
            .mockEndpoint()
            .post("/api/svc/v1/artifact-versions/signed-urls/multipart").jsonBody(rawRequestBody)
                .respondWith()
            .statusCode(200).jsonBody(rawResponseBody)
                .build();

        
                        
                                const response = await client.artifactVersions.createMultiPartUpload({
    id: "id",
    path: "path",
    numParts: 1.1
});
                                expect(response).toEqual({
    data: {
        storageProvider: "S3_COMPATIBLE",
        partSignedUrls: [{
                path: "path",
                signedUrl: "signed_url"
            }],
        finalizeSignedUrl: {
            path: "path",
            signedUrl: "signed_url"
        },
        s3CompatibleUploadId: "s3_compatible_upload_id",
        azureBlobBlockIds: ["azure_blob_block_ids"]
    }
});
                              
                    
    });
          
    test("list_files", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueFoundryClient({ "maxRetries" : 0 , "apiKey" : "test" , "environment" : server.baseUrl });
        const rawRequestBody = { "id" : "id" };
        const rawResponseBody = { "data" : [ { "path" : "path" , "is_dir" : true , "file_size" : 1.1 , "signed_url" : "signed_url" , "last_modified" : "2024-01-15T09:30:00Z" } ] , "pagination" : { "limit" : 10 , "nextPageToken" : "nextPageToken" , "previousPageToken" : "previousPageToken" } };
        
        server
            .mockEndpoint({ once: false })
            .post("/api/svc/v1/artifact-versions/files").jsonBody(rawRequestBody, { ignoredFields: [ "pageToken" ] })
                .respondWith()
            .statusCode(200).jsonBody(rawResponseBody)
                .build();

        
                        
                const expected = {
    data: [{
            path: "path",
            isDir: true,
            fileSize: 1.1,
            signedUrl: "signed_url",
            lastModified: new Date("2024-01-15T09:30:00.000Z")
        }],
    pagination: {
        limit: 10,
        nextPageToken: "nextPageToken",
        previousPageToken: "previousPageToken"
    }
};
                const page = await client.artifactVersions.listFiles({
    id: "id"
});
                
                            expect(expected.data).toEqual(page.data);
                            expect(page.hasNextPage()).toBe(true);
                            const nextPage = await page.getNextPage();
                            expect(expected.data).toEqual(nextPage.data);
                        
                
                    
    });
          
    test("stage", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueFoundryClient({ "maxRetries" : 0 , "apiKey" : "test" , "environment" : server.baseUrl });
        const rawRequestBody = { "manifest" : { "metadata" : { "key" : "value" } , "type" : "artifact-version" , "source" : { "type" : "truefoundry" } , "step" : 1 } };
        const rawResponseBody = { "id" : "id" , "storage_root" : "storage_root" , "artifact_id" : "artifact_id" };
        
        server
            .mockEndpoint()
            .post("/api/svc/v1/artifact-versions/stage").jsonBody(rawRequestBody)
                .respondWith()
            .statusCode(200).jsonBody(rawResponseBody)
                .build();

        
                        
                                const response = await client.artifactVersions.stage({
    manifest: {
        metadata: {
            "key": "value"
        },
        type: "artifact-version",
        source: {
            type: "truefoundry"
        },
        step: 1
    }
});
                                expect(response).toEqual({
    id: "id",
    storageRoot: "storage_root",
    artifactId: "artifact_id"
});
                              
                    
    });
          
    test("mark_stage_failure", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueFoundryClient({ "maxRetries" : 0 , "apiKey" : "test" , "environment" : server.baseUrl });
        const rawRequestBody = { "id" : "id" };
        const rawResponseBody = { };
        
        server
            .mockEndpoint()
            .post("/api/svc/v1/artifact-versions/mark-stage-failure").jsonBody(rawRequestBody)
                .respondWith()
            .statusCode(200).jsonBody(rawResponseBody)
                .build();

        
                        
                                const response = await client.artifactVersions.markStageFailure({
    id: "id"
});
                                expect(response).toEqual({});
                              
                    
    });
          
    test("get (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueFoundryClient({ "maxRetries" : 0 , "apiKey" : "test" , "environment" : server.baseUrl });
        
        const rawResponseBody = { "data" : { "created_at" : "2024-01-15T09:30:00Z" , "updated_at" : "2024-01-15T09:30:00Z" , "manifest" : { "name" : "name" , "metadata" : { "key" : "value" } , "ml_repo" : "ml_repo" , "version" : 1 , "type" : "artifact-version" , "description" : "description" , "version_alias" : "version_alias" , "source" : { "type" : "truefoundry" } , "step" : 1 , "run_id" : "run_id" } , "id" : "jqfwg345gi25n5ju2yz5iz6m" , "fqn" : "fqn" , "created_by_subject" : { "subjectId" : "subjectId" , "subjectType" : "user" , "subjectSlug" : "subjectSlug" , "subjectDisplayName" : "subjectDisplayName" , "subjectPatName" : "subjectPatName" , "subjectControllerName" : "subjectControllerName" , "subjectExternalIdentitySlug" : "subjectExternalIdentitySlug" } , "ml_repo_id" : "ml_repo_id" , "usage_code_snippet" : "usage_code_snippet" , "tags" : [ "tags" ] , "artifact_id" : "artifact_id" } };
        
        server
            .mockEndpoint()
            .get("/api/svc/v1/artifact-versions/id").respondWith()
            .statusCode(200).jsonBody(rawResponseBody)
                .build();

        
                        
                                const response = await client.artifactVersions.get("id");
                                expect(response).toEqual({
    data: {
        createdAt: new Date("2024-01-15T09:30:00.000Z"),
        updatedAt: new Date("2024-01-15T09:30:00.000Z"),
        manifest: {
            name: "name",
            metadata: {
                "key": "value"
            },
            mlRepo: "ml_repo",
            version: 1,
            type: "artifact-version",
            description: "description",
            versionAlias: "version_alias",
            source: {
                type: "truefoundry"
            },
            step: 1,
            runId: "run_id"
        },
        id: "jqfwg345gi25n5ju2yz5iz6m",
        fqn: "fqn",
        createdBySubject: {
            subjectId: "subjectId",
            subjectType: "user",
            subjectSlug: "subjectSlug",
            subjectDisplayName: "subjectDisplayName",
            subjectPatName: "subjectPatName",
            subjectControllerName: "subjectControllerName",
            subjectExternalIdentitySlug: "subjectExternalIdentitySlug"
        },
        mlRepoId: "ml_repo_id",
        usageCodeSnippet: "usage_code_snippet",
        tags: ["tags"],
        artifactId: "artifact_id"
    }
});
                              
                    
    });
          
    test("get (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueFoundryClient({ "maxRetries" : 0 , "apiKey" : "test" , "environment" : server.baseUrl });
        
        const rawResponseBody = { "key" : "value" };
        
        server
            .mockEndpoint()
            .get("/api/svc/v1/artifact-versions/id").respondWith()
            .statusCode(404).jsonBody(rawResponseBody)
                .build();

        
            await expect(async () => {
                return await client.artifactVersions.get("id")
            }).rejects.toThrow(TrueFoundry.NotFoundError);
    });
          
    test("delete (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueFoundryClient({ "maxRetries" : 0 , "apiKey" : "test" , "environment" : server.baseUrl });
        
        const rawResponseBody = { };
        
        server
            .mockEndpoint()
            .delete("/api/svc/v1/artifact-versions/id").respondWith()
            .statusCode(200).jsonBody(rawResponseBody)
                .build();

        
                        
                                const response = await client.artifactVersions.delete("id");
                                expect(response).toEqual({});
                              
                    
    });
          
    test("delete (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new TrueFoundryClient({ "maxRetries" : 0 , "apiKey" : "test" , "environment" : server.baseUrl });
        
        const rawResponseBody = { "key" : "value" };
        
        server
            .mockEndpoint()
            .delete("/api/svc/v1/artifact-versions/id").respondWith()
            .statusCode(404).jsonBody(rawResponseBody)
                .build();

        
            await expect(async () => {
                return await client.artifactVersions.delete("id")
            }).rejects.toThrow(TrueFoundry.NotFoundError);
    });
          
});
